# Software versions

This book was rendered from the source code on `r format(Sys.time(), "%b %d, %Y at %X")` with the following R package versions.

```{r session_summary, echo=FALSE, comment=NA}
  options(width = 60)
	sessionInfo()
```